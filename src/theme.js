import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette:{
    primary:{
      main: "#cdb4db",
      light: "#ffc8dd"
    },
    secondary:{
      main: '#ffafcc',
    },
    otherColor:{
      main:"#999"
    }
  }
})